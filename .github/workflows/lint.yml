name: Lint Workflows

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install shellcheck (optional)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends shellcheck

      - name: Download actionlint
        run: |
          mkdir -p bin
          curl -sSfL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash -o bin/download-actionlint.bash
          bash bin/download-actionlint.bash -b bin
          ./bin/actionlint -version

      - name: Run actionlint
        run: |
          ./bin/actionlint -color -verbose
